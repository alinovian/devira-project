{
  "name": "devira",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && mkdir -p public && cp -r *.html css/ js/ files/ contract/ assets/ public/",
    "predeploy": "npm install"
  },
  "dependencies": {
    "@metamask/sdk": "^0.1.0",
    "@pinata/sdk": "^2.1.0",
    "axios": "^1.10.0",
    "qrcode": "^1.5.4",
    "web3": "^1.7.0"
  }
}