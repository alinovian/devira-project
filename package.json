{
  "name": "devira",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "npm install --omit=dev",
    "build": "rmdir /s /q public & mkdir public & copyfiles *.html css/* js/* files/* contract/* assets/* public/",
    "start": "echo 'Aplikasi siap digunakan'"
  },
  "dependencies": {
    "@metamask/sdk": "^0.1.0",
    "@pinata/sdk": "^2.1.0",
    "axios": "^1.10.0",
    "qrcode": "^1.5.4",
    "web3": "^1.7.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "mkdirp": "^1.0.4"
  }
}